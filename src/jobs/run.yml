description: >
  A single, complete job to run.
executor: default

parameters:
  quality-check:
    description: This command check quality of code.
    type: boolean
    default: false

steps:
  - quality:
      quality-check: << parameters.quality-check >>
