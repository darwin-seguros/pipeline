description: >
  A single, complete job to run.
executor: default

parameters:
  quality_check:
    description: This command check quality of code.
    type: boolean
    default: false

steps:
  - quality:
      quality_check: << parameters.quality_check >>
